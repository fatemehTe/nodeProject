//CommonJS, every filr is module (by default)
//Modules - Encapsulated Code (only share minimum)

//Modules

const john = 'john'

const sayHi = (name)=>{
    console.log('Hello '+name)
}

sayHi('fateme')
sayHi(john)